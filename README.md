RDR2-Spot — Starter package

קבצים נוספים שנוצרו:
- docs/RDR2-Spot-SRS.md
- docs/Deliverables.md
- config/config.yaml.example
- .env.example

מה לעשות עכשיו:
1. ודא שאתה ב-workspace הנכון: `c:\Users\shh92\OneDrive\Documenti\BOT SPOT`
2. העתק `.env.example` ל-`.env` והכנס שם את המפתחות (רק אם אתה מאשר פעולה LIVE).
3. ערוך `config/config.yaml.example` ושמור כ-`config/config.yaml`.
4. אם תרצה, אבנה עכשיו מבנה פרויקט ראשוני (skeleton) עם main async loop + DB models (Dry-run first).

הערות בטיחות:
- לא ביצעתי שום commit או push ל-git.
- לא דרסתי קבצים קיימים — אם קיים שם קובץ בעל אותו שם, יצרתי אותו (או טרם יצרתי אם לא היה). בדוק אם אתה מעוניין ששמות ישתנו.

אם תרצה שאמשיך ואלמד את הקבצים כדי לייצר שלד פרויקט — אמור לי ואבנה את השלד כאן בלבד (ללא git push).